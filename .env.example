# eVuka REWARDS App - Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to version control!

# ============================================================================
# Supabase Configuration
# ============================================================================

# Supabase Project URL (CLIENT-SAFE)
# Get this from your Supabase project settings
VITE_SUPABASE_URL=https://your-project-url.supabase.co

# Supabase Anonymous Key (CLIENT-SAFE)
# This is safe to expose in client code - it's protected by Row Level Security
# Get this from Supabase Project Settings > API
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Project ID (BACKEND/CI ONLY)
# Used for CLI operations and type generation
# Do not prefix with VITE_ - not needed in browser
SUPABASE_PROJECT_ID=your-project-id

# ⚠️ CRITICAL: SUPABASE SERVICE KEY (SERVER-SIDE ONLY)
# ⚠️ NEVER expose this in client code or commit to git!
# ⚠️ This key bypasses ALL Row Level Security policies
# Only use this in:
#   - Supabase Edge Functions
#   - Backend servers
#   - CI/CD pipelines (as a secret)
# Remove VITE_ prefix so it's NOT bundled in client code
SUPABASE_SERVICE_KEY=your-service-key-here

# ============================================================================
# OpenAI Configuration
# ============================================================================

# ⚠️ OpenAI API Key (SERVER-SIDE ONLY)
# Do NOT use VITE_ prefix - this should only be used server-side
# Client requests should proxy through your backend
# If you must use AI features client-side, implement a backend proxy
VITE_OPENAI_API_KEY=your-openai-api-key

# ============================================================================
# WebSocket Configuration
# ============================================================================

# WebSocket Server URL (CLIENT-SAFE)
# URL for real-time leaderboard updates
VITE_WEBSOCKET_URL=https://your-websocket-server.com

# ============================================================================
# Feature Flags (CLIENT-SAFE)
# ============================================================================

# Enable AI-powered receipt analysis (requires backend API)
VITE_ENABLE_AI_ANALYSIS=true

# Enable duplicate receipt detection
VITE_ENABLE_FRAUD_DETECTION=true

# Enable OCR processing (Tesseract.js)
VITE_ENABLE_OCR=true

# Enable offline mode with IndexedDB
VITE_ENABLE_OFFLINE=true

# Enable PWA features
VITE_ENABLE_PWA=true

# ============================================================================
# Push Notifications - PWA (VAPID Keys)
# ============================================================================

# VAPID Public Key (CLIENT-SAFE)
# Used for web push notifications in the browser
# Generate VAPID keys with: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key-here

# ⚠️ VAPID Private Key (SERVER-SIDE ONLY)
# ⚠️ NEVER expose this in client code - used to send push notifications from server
# Store as a secret in your backend/Edge Functions environment
# Remove VITE_ prefix so it's NOT bundled in client code
VAPID_PRIVATE_KEY=your-vapid-private-key-here

# ============================================================================
# Monitoring and Logging (CLIENT-SAFE)
# ============================================================================

# Monitoring service endpoint (e.g., Sentry, DataDog)
VITE_MONITORING_ENDPOINT=https://your-monitoring-service.com/api/events

# Logging service endpoint
VITE_LOGGING_ENDPOINT=https://your-logging-service.com/api/logs

# Enable debug logs in console
VITE_DEBUG_LOGS=false

# Send logs to external services (disable during local development)
VITE_ENABLE_EXTERNAL_LOGGING=false

# ============================================================================
# Security (CLIENT-SAFE)
# ============================================================================

# Enable Content Security Policy headers
VITE_ENABLE_CSP=true

# ============================================================================
# Environment (CLIENT-SAFE)
# ============================================================================

# Environment name: development, staging, production
VITE_ENVIRONMENT=development

# ============================================================================
# Development Tools (CLIENT-SAFE)
# ============================================================================

# Enable Tempo DevTools routes
VITE_TEMPO=true

# ============================================================================
# SECURITY REMINDERS
# ============================================================================
#
# CLIENT-SAFE variables (prefixed with VITE_):
#   - These are bundled into your client JavaScript
#   - Safe to expose publicly
#   - Protected by other security measures (RLS, CORS, etc.)
#
# SERVER-ONLY variables (NO VITE_ prefix):
#   - Should NEVER be in client bundles
#   - Use only in backend/Edge Functions
#   - Store as secrets in deployment platform
#
# Best Practices:
#   1. Never commit .env file to git
#   2. Use different values for dev/staging/prod
#   3. Rotate keys regularly
#   4. Use deployment platform secrets for production
#   5. Never expose service keys, private API keys, or admin tokens
#   6. Audit your dist/ bundle to ensure no secrets leaked
#
# To verify no secrets in build:
#   npm run build && grep -r "your-secret-key" dist/
#
# ============================================================================
